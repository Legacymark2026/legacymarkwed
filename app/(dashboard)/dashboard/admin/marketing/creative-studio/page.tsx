import { CreativeStudio } from '@/components/marketing/creative-studio/creative-studio';

export const metadata = {
    title: 'Creative Studio | Torre de Control',
    description: 'Genera imágenes, videos y copy para tus campañas publicitarias con IA.',
};

export default function CreativeStudioPage() {
    return <CreativeStudio />;
}
