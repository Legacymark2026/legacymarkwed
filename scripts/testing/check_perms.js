const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function run() { console.log("COMPANY:"); const comp = await prisma.company.findFirst(); console.log(JSON.stringify(comp?.defaultCompanySettings, null, 2)); console.log("USERS:"); const cus = await prisma.companyUser.findMany({ include: { user: { select: { email: true, role: true } } } }); console.log(JSON.stringify(cus, null, 2)); } run().catch(console.error).finally(() => prisma.$disconnect());
